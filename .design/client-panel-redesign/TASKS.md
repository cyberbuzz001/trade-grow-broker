# Build Tasks: TradeGrow Client Panel Redesign

Generated from: `.design/client-panel-redesign/DESIGN_BRIEF.md`, `INFORMATION_ARCHITECTURE.md`, `DESIGN_TOKENS.md`
Date: 2026-08-21

Aesthetic direction established here: clean modern fintech (Groww/Zerodha/Upstox register), light-first, calm and authoritative — established visually starting at Task 2 (primitives) and Task 5 (AppShell), before any individual page is touched, so the direction can be validated early.

## Foundation

- [x] **Router & route table**: Installed `react-router-dom`. Replaced `App.tsx`'s `activeSubView`/`activeMobileTab`/`isMobileScreen` state-switch with a real route tree matching the IA site map (`/`, `/watchlist`, `/option-chain`, `/portfolio/*`, `/profile/*`, `/admin/*`, plus `/terminal` for the old `isTerminalMode` boolean), including the `/portfolio→/portfolio/positions` and `/profile→/profile/account` redirects. Preserved existing auth gating (unauthenticated → `AuthModal`, non-staff → Access Denied panel on `/admin/*`) as route-level checks. One deliberate, small behavior change: the Stocks/F&O/Commodities category filter used to override *every* tab's content platform-wide when set to Commodities (a pre-existing quirk); it's now scoped to the Explore/Home route only, matching the already-approved IA — everything else is byte-for-byte the same child components as before. _Modifies: `App.tsx`, `package.json`. Verified via a real headless-browser smoke test (Playwright + Chromium, installed this session — no interactive browser was available otherwise): login, every nav click, browser back button, refresh-preserves-page, direct URL entry (including a redirect and a nonsense path), and the mobile viewport all confirmed working with zero console errors from this change. Screenshots saved in the scratchpad. Found and flagged (not fixed — out of scope) one unrelated pre-existing bug: `UserProfileModal.tsx`'s UPI-link fetch has no `isOpen` gate and fires on every page load._

- [x] **UI primitives — static set**: Built `components/ui/Button.tsx`, `Card.tsx` (+ `CardHeader`/`CardTitle`), `Badge.tsx`, `Tabs.tsx` on the token system from `DESIGN_TOKENS.md` (using `lucide-react` for icons, `clsx` for variant composition — both already installed, no new dependencies). Button: primary/secondary/ghost/destructive variants, sm/md/lg/icon sizes (icon size enforces a 44×44px minimum touch target), loading state, `focus-visible`-only ring via `--shadow-focus` (not on mouse click). Badge: gain/loss/warning/info/neutral/primary variants with an optional dot indicator, documented to never be the only signal of meaning. Tabs: one horizontally-scrollable pill-row implementation for every tabbed page, full keyboard support (arrow keys/Home/End move focus and selection, proper `role="tablist"`/`aria-selected`). _Reuses: `.tg-card`/`.tg-pill-btn` CSS patterns as the visual reference; New: the components themselves. Caught and fixed one bug in my own first draft before shipping it — Button's icon-only sizing path had a condition that would silently drop `children` (including screen-reader label content) whenever `leftIcon` was also passed; simplified to one consistent render path. Verified via a clean Docker build (`tsc` + `vite build`, zero errors) — no page imports these yet (that's Task 5), so there's nothing to screenshot yet; real visual verification happens once AppShell wires them into the live app next._

- [x] **UI primitives — Dialog/Sheet**: Built `components/ui/Dialog.tsx` — one component, centered modal at `sm:` (640px) and above / bottom sheet below it via CSS alone (no viewport-detection prop needed), portal-rendered to `document.body`, using the `--overlay-backdrop` token, with a real focus trap (Tab/Shift+Tab cycle within the dialog, wrapping at both ends), return-focus-to-trigger on close, Escape-to-close, backdrop-click-to-close, and a body-scroll lock while mounted. _Depends on: UI primitives — static set (uses `Button` for its close/footer buttons). New component; supersedes the ad hoc backdrop classes noted in `DESIGN_TOKENS.md` once modals migrate to it. Verified empirically, not just by type-check — this primitive has real DOM/focus/animation logic worth confirming works, unlike Task 2's static components: temporarily wired a debug trigger into both the desktop and mobile shells, ran a Playwright script covering focus-moves-in-on-open, Shift+Tab wrapping to the last element, Escape closing and returning focus to the trigger, body scroll lock/unlock, backdrop-click-to-close, and the responsive presentation switch (confirmed via bounding box that the mobile variant reaches the bottom of the viewport, the desktop variant is centered) — all passed. Screenshots saved in the scratchpad. Removed the temporary debug wiring afterward and confirmed `App.tsx` reverted to byte-identical with its pre-test state before redeploying._

- [x] **UI primitives — DataTable**: Built `components/ui/DataTable.tsx` — a real `<table>` at `md` (768px) and above, a stacked-card list below it, both driven by the same generic `columns`/`rows` props (one designates `mobilePrimary` for the card title, others become label/value pairs, `mobileHidden` drops a column from the card only). Handles loading (skeleton rows, reusing the existing `.skeleton-shimmer` utility), empty state, and an optional `renderMobileActions` footer slot. _Depends on: Badge (status pills in cells) — done. New component; directly replaces the `overflow-x-auto`-only / `overflow-hidden`-clipped table patterns found in `OrdersPositionsView.tsx` and (later, admin phase) `CustomerList.tsx`. Verified empirically with sample position-like data (gain/loss badges, mixed qty signs) via a temporary test route — caught and fixed a real test-harness bug in the process (the desktop and mobile route trees in `App.tsx` have different indentation, so my first patch attempt silently only added the test route to one of the two, and the failed run's row count came from an unrelated table already on the fallback page it redirected to instead — worth remembering for the remaining tasks, since both trees need touching separately). Confirmed: correct column alignment, badge rendering, `hidden md:block` correctly hides the table on mobile, all 3 sample rows render as cards with the right title/field mapping. Screenshots saved in the scratchpad. Reverted all temporary wiring, confirmed byte-identical to pre-test `App.tsx`, redeployed clean._

- [x] **AppShell**: Built one responsive shell — a top nav bar on desktop, a bottom tab bar on mobile/tablet-portrait — same 5 primary items + conditional Admin (Explore, Watchlist, Option Chain, Portfolio, Profile). **Corrected mid-build**: this task originally called for a left nav rail per the IA, but that didn't actually match the stated Groww/Zerodha/Upstox reference (all three use top navigation) — flagged to and confirmed with the designer before building, IA updated to match. Carries forward the ticker strip and index-click-to-chart feature from the old `GrowwSubNav`, and the wallet pill/theme toggle/profile menu/admin badge from the old `GrowwHeader`, consolidated into one component. The Stocks/F&O/Commodities category pill is intentionally NOT part of this shell (per the IA, it belongs on the Explore/Watchlist pages themselves) — it has no UI anywhere until Task 6 restores it there; the underlying `?category=` mechanism from Task 1 still works via direct URL in the meantime. _Depends on: Router, UI primitives — both done. Modifies/replaces: `GrowwHeader.tsx`, `GrowwSubNav.tsx`, `mobile/MobileBottomNav.tsx` (all three kept on disk, unused, until the cleanup task). Caught and fixed three real bugs before shipping: (1) `IndexActionModal`'s `onOpenChart` callback passes `(symbol, token, exchange)` — my first draft discarded them and just toggled terminal mode generically, meaning clicking "view chart" on Bank Nifty would've shown whatever symbol was last set instead of Bank Nifty; (2) the profile avatar button had no `aria-label`, just a bare letter — a real accessibility gap, not just a test-selector inconvenience; (3) the top bar visually overflowed at 1440px width (confirmed via a Playwright bounding-box check, not just eyeballing a screenshot) — nav labels wrapped and the profile avatar was clipped at the edge. Fixed by tightening paddings/gaps and pushing lower-priority labels (search box, Terminal/Admin text) to appear only at wider breakpoints; reverified 1440px and 1280px now render with zero overflow, 1024px/768px fall back to a horizontal scroll (acceptable — not worth degrading the two far more common widths to fully fit the rarer narrow ones). All nav clicks, active-state highlighting, terminal toggle (including toggling off via browser-back, a free improvement from real routing), theme toggle, and the profile menu verified via Playwright on both desktop and mobile._

## Core UI

- [x] **Explore (Home) page**: Rebuilt `GrowwExploreView.tsx` as one responsive component (no separate mobile version) at route `/`, on the new primitives (Card, Badge, Tabs, DataTable). The Stocks/F&O/Commodities category pill is real now — wired to `?category=` query param, F&O shows a genuinely populated (if intentionally light — the full dedicated experience is Task 8's job) contract list instead of doing nothing, Mutual Funds is gone entirely. `MobileHomeView` is now fully superseded (its unique content — the market-indices grid, since `AppShell`'s ticker is desktop-only — was folded into this page; its redundant header/logo/search/theme-toggle chrome was simply dropped, since `AppShell` already provides all of that). _Depends on: AppShell, primitives — both done. Modifies: `GrowwExploreView.tsx`, `App.tsx` (both `/` routes now point here; the `HomeRoute` wrapper and `MobileHomeView` import removed). Removed: the `MUTUAL_FUNDS` category entirely from active code (confirmed via grep — only the dead, unused `GrowwHeader.tsx` still references it, correctly left alone for the cleanup task)._ Caught and fixed two real bugs before shipping: (1) a dynamically-constructed Tailwind class (`` `bg-${tool.color}-500/15` ``) in the Products & Tools list — Tailwind's JIT compiler can't see template-literal class names at build time, so those icons would've rendered with no color at all in production; fixed by using static per-item class strings. (2) A genuine dead-end: selecting Commodities returned `McxCommodityView` early, which also removed the category Tabs control itself, so there was no way back to Stocks/F&O once you clicked Commodities — caught by a Playwright test that clicks through Stocks→F&O→Commodities→back-to-Stocks, not by inspection. Fixed by always rendering the Tabs control and only swapping the content below it. Verified via Playwright: category switching (including the fixed dead-end), movers tab switching, index-card click opening the modal, and mobile rendering — a mobile screenshot artifact (bottom nav appearing to overlap content) was double-checked against a real scrolled viewport capture and confirmed to be a `fullPage`-screenshot stitching artifact with `position: fixed` elements, not a real bug.

- [x] **Watchlist page**: Rebuilt `GrowwWatchlistView.tsx` as one responsive component at `/watchlist`, on `DataTable`/`Tabs`/`Card`/`Badge`/`Button`. **Corrected the IA on this one, having actually read the existing code**: the IA called for reusing Explore's Stocks/F&O/Commodities instrument-universe pill here too, but Watchlist's existing All/Stocks/Indices/Commodities filter is a genuinely different feature — it filters *what you've already tracked* by asset type, not *which market you're browsing*. Bolting the Explore-style pill on top would've been a second, confusing, redundant control. Instead extended the existing tracked-item filter with an `F&O` option (previously impossible — no F&O category existed at all) and seeded 3 real F&O contracts into the default/search catalog so the filter isn't another empty dead-end. `DataTable` absorbed nearly all of the old mobile-vs-desktop duplication for free — the responsive table/card split, previously ~350 lines of hand-rolled mobile card markup in `MobileWatchlistView.tsx`, is now just column definitions. _Depends on: AppShell, primitives, DataTable — all done. Modifies: `GrowwWatchlistView.tsx`, `App.tsx` (mobile `/watchlist` route now points here; `MobileWatchlistView` import removed, though note `App.tsx` gained a small amount of new state — `mobileWatchlistChart` — to keep mobile's "tap a row to view its chart" behavior working, since that's a platform-appropriate-action decision the page component deliberately leaves to its caller, matching the same pattern used for Explore's index clicks in Task 6). Caught one real regression before shipping: my first pass dropped the `onSelectSymbolForTerminal` wiring for mobile entirely, silently losing the existing tap-a-row-to-view-chart feature `MobileWatchlistView` had — caught by comparing against the original file rather than trusting the merge, fixed by widening the callback to pass `(symbol, token, exchange)` so the caller can open either a Terminal navigation (desktop) or `MobileChartModal` (mobile) as appropriate. Order placement still goes through the pre-existing `OrderPreviewModal` unchanged — deliberately not migrated to the new `Dialog` primitive yet, since that's Task 12's job, not this one. Verified via Playwright: filter switching (including the new F&O option), search-and-add, BUY opening the order modal, Remove, row-click-to-terminal on desktop, and row-tap-to-chart-modal on mobile (confirmed via screenshot, not just a passing assertion) — all correct.

- [x] **F&O view (new)**: Built `FoHubView.tsx` — quick-access cards for NIFTY/BANKNIFTY/FINNIFTY/SENSEX (live price + change, click deep-links into `/option-chain?symbol=X` with that underlying pre-selected), a Popular F&O Contracts grid, and a primary "Open Full Option Chain" CTA. Wired into `GrowwExploreView.tsx`'s `category === 'FO'` branch, replacing Task 6's lightweight interim (a plain contracts grid reusing the Stocks layout). Added minimal, additive `?symbol=` query-param support to `OptionChainView.tsx` (defaults to the existing `'SENSEX'` behavior when absent — Task 9 still owns the full rebuild of this file). _Depends on: Explore/Watchlist category wiring — done. New file; deliberately does not duplicate `OptionChainView`'s chain/strike/order logic, just curates entry points into it._ Caught and removed a real problem in my own first draft before shipping: two "Long/Short Build-up Scanner" cards that looked like real features but had no click handler and no data behind them — exactly the kind of looks-real-does-nothing UI this whole redesign exists to fix, not add. Verified via Playwright (with a brief detour: my first test run's "BANK NIFTY" click hit `AppShell`'s ticker strip instead of the hub's own card, since both render the same text — confirmed by counting matches and checking outerHTML, not by assuming; the actual hub card correctly navigates to `/option-chain?symbol=BANKNIFTY`). Confirmed: quick-access cards, popular-contract clicks, and the generic CTA all navigate correctly with the right underlying pre-selected; the Stocks tab still works after visiting F&O (no new dead-end); mobile renders cleanly as a 2-column grid.

- [x] **Option Chain page**: This page already had a real CSS-driven responsive split (`hidden md:table` desktop / `md:hidden` mobile compact table in the same file — never two separate components), so no mobile/desktop merge was needed. The actual problem, found on read: `OptionChainView.tsx` and `OptionChainRow.tsx` were hardcoded to a single dark slate palette (`slate-900/950/800`, raw `text-white`) with zero light-theme awareness — the only page in the whole redesigned app ignoring the theme toggle entirely. Converted systematically to the CSS variable tokens (`--bg-surface`, `--bg-surface-inset`, `--border-color`, `--text-main`/`--text-muted`), and — since dedicated tokens already exist for exactly this purpose — CE/PE accent colors and ITM-highlight tints now use `--call-accent`/`--put-accent`/`--call-bg-itm`/`--put-bg-itm`/`--call-bg-hover`/`--put-bg-hover` instead of raw cyan/purple Tailwind classes. Solid-color BUY/SELL/Chart action buttons deliberately left untouched (self-contained, correct in both themes already). Migrated the strike-click order-preview flow onto the new `Dialog` primitive per this task's own scope — refactored `OrderPreviewModal.tsx`'s outer chrome (previously a hand-rolled fixed-backdrop div with no close animation) to render through `Dialog`, keeping 100% of its business logic (margin quote fetch, order submission, FORM/PLACING/SUCCESS/REJECTED step machine) unchanged; this improves `GrowwWatchlistView.tsx`'s order flow too (still using `OrderPreviewModal`) for free. _Depends on: AppShell, Dialog/Sheet primitive — both done. Modifies: `OptionChainView.tsx`, `OptionChainRow.tsx`, `OrderPreviewModal.tsx`. Added a small, genuinely-needed capability to the `Dialog` primitive itself in the process: a `dismissible` prop (default true) — the original modal deliberately couldn't be dismissed via Escape/backdrop during order submission (an already-in-flight API call), and `Dialog` didn't support that distinction before this; also widened `title` from `string` to `ReactNode` since this modal's headers are colored badges, not plain text. Verified with a genuinely delayed network response (not just a short timeout guess) that Escape and backdrop-click are both correctly blocked while an order is actually in-flight, and correctly work again once it resolves — confirmed both light and dark theme render correctly (screenshots), and that a real order placed through the new Dialog-based flow succeeds end-to-end (order + position rows created, cleaned up afterward)._

- [x] **Portfolio → Positions & Orders**: The biggest consolidation in this list — merged `OrdersPositionsView.tsx` (1213 ln, desktop) and `mobile/MobilePortfolioView.tsx`'s positions/orders/closed-trades handling (1066 ln, mobile) into one 599-line responsive component on `DataTable`/`Tabs`/`Dialog`/`Card`, routed at `/portfolio/positions`, `/portfolio/orders`, and a new `/portfolio/history` (added mid-build, see below). JS bundle dropped ~940KB → 896.83KB confirming real deduplication, not just a file merge. **Three design corrections made mid-build, all from reading both source files rather than trusting the original task description**: (1) discovered a genuine third data shape — a *closed trade* (settled entry/exit pair with realized P&L and an exit reason like `RMS_AUTO_SQUARE_OFF`) is not the same shape as an open position (net qty/avg price/live LTP) or an order-book row. The pre-merge mobile version folded closed trades into the Positions list via a filter, forcing its row renderer to branch on two incompatible shapes; the pre-merge desktop version kept a separate Trade History tab, which is the cleaner design — adopted it for both platforms and added `/portfolio/history` as a real IA-documented 5th Portfolio tab (`INFORMATION_ARCHITECTURE.md` updated with the reasoning). (2) Positions tab now shows OPEN positions only (`netQty !== 0`) — dropped the old ALL/OPEN/CLOSED filter entirely, since "closed" now correctly lives in History instead of being a filter state on the same list. (3) Dropped a third, independent duplicate Holdings implementation found inline inside `OrdersPositionsView.tsx` itself (on top of the standalone `GrowwHoldingsView.tsx`) — out of scope here, correctly belongs to and is already owned by the next task. _Depends on: AppShell, DataTable, Router (tab-as-route) — all done. Modifies: `OrdersPositionsView.tsx`, `App.tsx` (both desktop and mobile route trees — three explicit routes now, all passing `onOpenOptionChain` for the "view chart" deep-link). Supersedes: `MobilePortfolioView.tsx`'s positions/orders/closed-trades portion (file kept on disk, unused, until the dead-code-removal task)._ Verified via Playwright against two synthetic MIS positions on throwaway test accounts (desktop 1440px + mobile 390px, light + dark theme): open position renders correctly with live LTP/unrealized P&L; the 2-step Set Target flow (price entry → confirmation step with computed target P&L → real LIMIT order placed) works end-to-end and is visible in the Orders tab; Cancel Target's confirm dialog correctly warns the position stays open; Square Off's confirm dialog correctly submits a MARKET order in the right direction/qty (confirmed via DB, not just the UI's own success message) and the position disappeared from the desktop test account (order filled against a live tick within ~1s); Orders and History tabs render their empty and populated states correctly; mobile card layout, bottom-tab-bar, and mobile-specific action buttons (Target/Square Off inline on the card) all work, including a real bug caught in my own Playwright script (not the app) — `DataTable` intentionally renders both the desktop `<table>` and the mobile card list in the DOM simultaneously, hidden by breakpoint via CSS, so an un-scoped `button:has-text(...)` locator grabs the hidden desktop copy first at mobile viewport widths; fixed by scoping to `:visible`. Zero console/page errors on either viewport. One pre-existing, non-regressed issue found and deliberately left alone (present verbatim in the original file, confirmed against the backup): the square-off success message says "...order executed..." based only on the order being *accepted*, not an actual fill confirmation — a second test account's square-off order sat `ACCEPTED` for 90+ seconds waiting on a live market tick for RELIANCE before I cleaned it up, during which the UI had already (incorrectly) reported it as executed. Flagged as a follow-up, not fixed here — it's an order-fill-messaging semantics question across the whole OMS, not a Task 10 duplication/consolidation issue._

- [x] **Portfolio → Holdings & Analytics**: Replaced `GrowwHoldingsView.tsx` (97 ln — turned out to be 100% hardcoded fake markup: "HOLDINGS (0)"/₹0 static text with no fetch, no props for real data, and a "Track external stocks" button with no `onClick` at all) and `PortfolioAnalyticsView.tsx` (194 ln — fetched real positions/holdings into state but never rendered either; a "Capital Asset Allocation" panel showing fixed 45/30/25% regardless of the account; a hardcoded "INITIAL CAPITAL: ₹10,00,000.00" line; and 100%-hardcoded dark-slate classes ignoring the theme toggle entirely) with one real, data-driven `PortfolioHoldingsAnalyticsView.tsx`, routed at `/portfolio/holdings` and `/portfolio/analytics`. **Two real problems found during the build, not in the original task description, both fixed here:** (1) Holdings/Analytics had **zero route on mobile at all** — direct dead ends, unreachable except by an admin who already knew to add them; both routes now exist on mobile too. (2) The 5 Portfolio tabs (Positions/Orders/History, owned by `OrdersPositionsView.tsx`, and Holdings/Analytics, owned by this new file) had no way to navigate between the two components — each only rendered its own subset of tabs via local component state, so a user on Positions had no in-page way to reach Holdings short of typing a URL. Fixed by extracting a shared `PortfolioNav.tsx` (route-driven via `useNavigate`, not local state — the URL is now the single source of truth for the active tab, also fixing a latent Task-10 bug where clicking a tab updated the displayed content but not the URL) used by both components, so all 5 tabs are reachable from any of them. **A third, more consequential correction**: read both the original IA and the actual backend and found they disagreed — the IA's own Positions content-hierarchy line listed a CNC badge for Positions, while its Naming Conventions table (a few lines below, apparently written independently) explicitly says CNC must never be called "Positions," only "Holdings." Backend investigation confirmed this isn't just a wording nitpick: a CNC fill is written to *both* the `positions` table and a separate `holdings` table by `PortfolioService.recordExecutionInTransaction`, so showing CNC positions in the Positions tab as well as Holdings would surface the same real position twice under two different names — the CNC-position-and-holdings sighting is the third real duplicate found in the app so far (after Task 10's inline third Holdings tab and the mobile/desktop file pairs generally). Resolved in favor of the Naming Conventions table: `OrdersPositionsView.tsx`'s Positions tab now filters out `productType === 'CNC'`; `INFORMATION_ARCHITECTURE.md` updated with the correction and the reasoning. Holdings tab: real `/api/v1/portfolio/holdings` data in a `DataTable` (Instrument/Qty/Avg Price/LTP/Current Value/P&L), a real Invested-vs-Current-value summary card (previously entirely fake). Analytics tab: kept the metric cards that were already real (wallet-derived Total Value/Buying Power/Margin/Combined P&L), converted to CSS tokens; replaced the fake 45/30/25% allocation bars with a real breakdown computed from actual holdings + open non-CNC positions + cash; dropped the unverifiable "INITIAL CAPITAL: ₹10,00,000.00" line; added the RMS reduce-only banner using the exact substantive wording RMS.ts itself returns on a rejected order ("restricted to reduce-only trading pending risk review") so the language matches once Task 14 wires the same text into the order-rejection path. Two small, additive backend changes needed to make this real rather than another mock: `GET /api/v1/auth/me` now also returns `user.riskRestriction` (was tracked and enforced server-side since the earlier RMS Phase 2 work, but never exposed to any client) and a new `GET /api/v1/risk-info` returns the 3 non-secret system-setting values (leverage multiplier, auto-square-off time/enabled) the Analytics meter needs, so those figures are live-configured instead of hardcoded text that would silently drift from whatever an admin actually sets via `/admin/risk-settings`. _Depends on: Portfolio → Positions & Orders (shares the tab shell, now via `PortfolioNav`). Modifies: `OrdersPositionsView.tsx` (CNC filter, `PortfolioNav` adoption, local tab state removed), `App.tsx` (both route trees; 2 new mobile routes), `server/src/routes/api.ts` (`/auth/me` addition, new `/risk-info`), `client/src/types/index.ts` (`User.riskRestriction`). New: `PortfolioHoldingsAnalyticsView.tsx`, `PortfolioNav.tsx`. Removes: `GrowwHoldingsView.tsx`, `PortfolioAnalyticsView.tsx` imports (files kept on disk, unused, until the dead-code-removal task)._ Also fixed, found only once real usage pushed the tab count from 3 to 5: the shared `Tabs` primitive had no scroll-into-view behavior, so on mobile the active tab could land scrolled out of the visible row with no indication which tab was selected — added a `scrollIntoView` effect keyed on the active value directly in `Tabs.tsx`, benefiting Task 12's 8-tab Profile page too, not just this one. Verified via Playwright (desktop 1440px + mobile 390px, light + dark): CNC position confirmed absent from Positions and present only in Holdings; cross-navigation between all 5 tabs via clicking (not just direct URL) confirmed on both platforms; direct URL entry to `/portfolio/holdings` on mobile confirmed working (previously would have been a blank/unrouted page); Reduce-Only banner confirmed rendering with real backend-sourced `riskRestriction` state; real leverage/cutoff figures confirmed matching the actual `system_settings` row values, not the old static text; allocation percentages confirmed computed from real holdings/positions/cash, not fixed 45/30/25; zero console/page errors (one pre-existing, already-flagged-in-Task-1 401 from `UserProfileModal`'s ungated UPI-link fetch re-observed, not a regression)._

- [x] **Unified Profile page**: Built `ProfilePage.tsx` at `/profile/:tab` with all 8 real routed tabs (Account, KYC, Bank, Security, Funds, Support, Permissions [staff-only], Appearance), on `Card`/`Tabs`/`Badge`/`Button`/`DataTable`/`Dialog`, fully theme-aware. **What this task actually found was worse than "two duplicate profile UIs"**: reading both source files plus every caller of `openProfileModal`/`onOpenProfileModal` turned up **five independent implementations of the same deposit/withdrawal/support features**, not two — (1) `ClientProfileView.tsx`'s route-based BANK tab (withdrawal only, no deposit), (2) `UserProfileModal.tsx`'s modal-based FUNDS tab (deposit+withdrawal+real LinkPe UPI QR flow, richer than #1), (3) `MobileProfileView.tsx`, a mobile hub screen whose every row just opened #2 as an overlay, (4) `LinkPeAddFundsModal.tsx` (702 ln), a *third* independent deposit/withdrawal+LinkPe implementation wired to `AppShell`'s wallet-balance-pill click, and (5) `CustomerSupportModal.tsx` (254 ln), a *fourth* independent "create ticket / view my tickets" implementation hitting the same `/api/v1/support/tickets` endpoint UserProfileModal's own SUPPORT tab already covered. All five collapsed into this one file's Funds and Support tabs; the wallet-pill and every `onOpenProfile(...)` call site now navigate to a real `/profile/:tab` route instead of opening a modal. Also found and fixed: `UserProfileModal`'s type signature declared a `PERMISSIONS` tab that was never implemented anywhere (no tab button, no content block, a genuine dead type) — built for real here by reusing the existing, already-complete `PermissionsDashboard.tsx` (740 ln, already used in `AdminPanel`) directly, rather than rebuilding it a second time. **A second, more consequential bug found while verifying**: `ClientProfileView.tsx` never read the `:tab` URL param at all — every `/profile/*` URL on desktop rendered the identical page defaulted to its KYC tab, so `/profile/account`, `/profile/security`, etc. were all silently the same dead link. `ProfilePage` now reads `useParams` and redirects to a valid tab (falling back to `account`, and away from `permissions` for non-staff) so the URL is the real source of truth. **A third, unrelated but high-severity bug found only because this task's own verification insisted on testing real `page.goto()` direct-URL entry (not just in-app clicks)**: `App.tsx`'s login handler never persisted the auth token to `localStorage` — only `refreshToken` was stored, so any hard refresh or direct URL navigation silently logged the user out despite a valid session, even though `handleLogout` was already correctly clearing that same key. Fixed with a one-line `localStorage.setItem('token', t)` alongside the existing `setToken(t)` call. _Depends on: AppShell, Tabs, Dialog, DataTable, Router — all done. New: `ProfilePage.tsx`. Modifies: `App.tsx` (removed `isProfileModalOpen`/`isSupportModalOpen`/`isLinkPeModalOpen`/`profileInitialTab` state and all three modal renders on both platforms; wallet-pill and `openProfileModal` callers now navigate; the token-persistence fix). Supersedes: `ClientProfileView.tsx`, `UserProfileModal.tsx`, `MobileProfileView.tsx`, `LinkPeAddFundsModal.tsx`, `CustomerSupportModal.tsx` (all five kept on disk, unused, until the dead-code-removal task)._ JS bundle dropped 898.27KB → 801.31KB, confirming real deduplication of five overlapping implementations, not just a file shuffle. Verified via Playwright (desktop 1440px + mobile 390px, light + dark, one non-staff client account and one staff `SUPPORT_AGENT` account): every one of the 8 tabs reachable both by direct URL and by clicking through the tab bar (a real ambiguous-locator test bug was caught and fixed along the way — `button:has-text("Bank")` matched the ticker strip's "BANK NIFTY" index button before the Profile page's own "Bank" tab, since AppShell's ticker renders earlier in the DOM — same class of bug Task 8 already hit once with the same two words); Funds tab's real LinkPe QR renders and the fund-request history table works; Support tab ticket submission appears immediately in its own history table; Account tab save + the renamed, confirm-dialog-gated "Reset Capital" action (previously "Refresh Account Balance", a misleading label for what is actually a full wallet reset) both work; Permissions tab correctly hidden and route-redirected for a non-staff user, and correctly renders the real `PermissionsDashboard` for a staff user; wallet-pill click deep-links straight to `/profile/funds`. Zero console/page errors on either viewport._

## Interactions & States

- [x] **Order placement flow**: Consolidated onto one `OrderPreviewModal.tsx`, already Dialog-based since Task 9. **Reading both files first changed the scope of "consolidate" here**: `OrderPreviewModal` was already the one shared flow for equities *and* options across 3 desktop call sites (`GrowwWatchlistView`, `OptionChainView`, and a dead `TradingTerminal.tsx` — see below) — desktop equity orders already passed dummy `expiry:''/strike:0/optionType:'CE'` through the same options-shaped `OrderPreviewDetails`, so the real duplicate was only `mobile/MobileOrderModal.tsx`: a hand-rolled fixed-backdrop sheet (not on `Dialog` at all), with its own naive client-only margin math (`orderValue / leverage`, no server margin-quote call, no available-funds check at all) instead of the real SPAN-aware `/api/v1/margin/quote` the desktop flow already used. It also offered a third "Cover (CO)" product-type button that isn't a real backend product type at all — `OMS`/`RMS`/`MarginMath` only recognize `MIS`/`CNC`/`NRML` — selecting it silently submitted as MIS with no indication of the substitution. Replaced entirely: `App.tsx`'s mobile quick-order flow (search result tap, chart BUY/SELL) now opens the same `OrderPreviewModal`, extended with two small additive props — `sideEditable` (renders an in-sheet BUY/SELL toggle for entry points that haven't already committed to a side, e.g. a search result tap) and `riskRestriction` (renders the reduce-only inline block, disables Confirm, using the exact wording established in Task 11's Analytics banner — this modal only ever opens new exposure, never squares off an existing position, so `REDUCE_ONLY` always blocks it outright here). Also added, since TASKS.md's own description called for it: the margin-insufficient warning now actually disables the Confirm button (previously showed a red "Shortfall Alert" but still let the click through) and a "Add funds in Profile → Funds" link that navigates there directly. Converted the remaining hardcoded `emerald-600 dark:emerald-400` / `rose-600 dark:rose-400` pairs Task 9 had left behind to `--gain`/`--loss` tokens, and swapped the hand-rolled status pills for the shared `Badge` primitive. **Three real bugs found and fixed while rewiring the mobile call sites**, none previously flagged: (1) `MobileChartModal`'s BUY/SELL button already passed a `side` argument up to `App.tsx`, which silently dropped it before opening the order sheet — tapping SELL on a chart always opened defaulted to BUY; (2) the global-search quick-order path swapped its own `token`/`symbol` fields (passed the instrument token in as the order's `symbol`) and hardcoded a fake `₹297.64` price regardless of the actual instrument selected; both are now correct — `symbol` is the real display symbol, price resolves from the live `ticks` map keyed by the real token, falling back to whatever the caller already had. **One dead file found in passing, not previously on the known-dead-files list**: `TradingTerminal.tsx` (threaded `riskRestriction` through it for consistency before discovering) has zero importers anywhere in the app — `GrowwTerminalView.tsx` is the actual live terminal and doesn't place orders through either modal at all; flagged for Task 15's cleanup alongside the Mobile*View files. _Depends on: Dialog/Sheet, Option Chain page — both done. Modifies: `OrderPreviewModal.tsx` (sideEditable/riskRestriction props, disable-on-shortfall, token conversion), `App.tsx` (mobile quick-order state and all 3 call sites), `GrowwWatchlistView.tsx`/`OptionChainView.tsx`/`TradingTerminal.tsx` (new optional `riskRestriction` prop threaded from `user.riskRestriction`). Supersedes: `mobile/MobileOrderModal.tsx` (kept on disk, unused, until the dead-code-removal task)._ JS bundle dropped 801.31KB → 791.72KB. Verified via Playwright (desktop 1440px + mobile 390px): Watchlist and Option Chain BUY flows confirmed still using the shared modal correctly; a real low-funds test account confirmed Confirm disabled + Add-funds link navigating to `/profile/funds`; a real `REDUCE_ONLY` test account confirmed the inline block, exact wording, and a disabled `RESTRICTED` button in place of Confirm; mobile quick-order via global search confirmed the correct symbol (not a raw token), a live resolved price (not the old hardcoded 297.64), and the new BUY/SELL toggle all render correctly, with the resulting order's side actually reaching the backend request. Zero console/page errors on either viewport._

- [x] **RMS restriction & risk-tier surfacing**: Both of this task's two named deliverables were already built ahead of schedule — the Portfolio → Analytics banner shipped in Task 11 (it's what that task's own "add the RMS risk-tier/reduce-only banner" line called for), and the order-flow inline block shipped in Task 13 (also explicitly named in that task's own description) — both using the identical substantive wording, confirmed here by grepping all three client-side surfaces against the backend's own `RMS.ts` rejection string: `"...restricted to reduce-only trading pending risk review"` matches byte-for-byte everywhere (the client versions drop only the `ORDER_REJECTED:` error-code prefix, appropriate since that prefix is meaningful on a rejected order response, not a proactive banner). What this task actually found still missing, re-reading the IA's own "Check and act on a restricted/at-risk account" user flow line by line: step 3, "User navigates to Portfolio → Positions, sees the affected position flagged" — `OrdersPositionsView.tsx` never received `riskRestriction` at all, so a restricted user saw the Analytics banner but nothing on the page where their positions actually live. Added the identical banner there (visible on Positions/Orders/History alike, not just Positions — a restricted account matters regardless of which of the three tabs is open), rather than a per-row badge, since the restriction is a real account-level RMS state, not tied to any one position — flagging individual rows would misrepresent it as position-specific. _Depends on: Portfolio → Holdings & Analytics, Order placement flow — both done. Modifies: `OrdersPositionsView.tsx` (new `riskRestriction` prop + banner), `App.tsx` (threaded `user?.riskRestriction` into all 6 `OrdersPositionsView` call sites, desktop + mobile × 3 tabs)._ Verified via Playwright with a real `REDUCE_ONLY` test account holding an open MIS position: banner renders on both Positions and Orders tabs with the exact established wording; confirmed the RMS square-off exemption still works correctly from the client side while restricted — Square Off on the flagged position succeeded (not blocked), matching the backend's own "reduce, don't open" semantics the banner describes. Zero console/page errors._

- [x] **Dead code removal**: Deleted 15 superseded files total — more than the original list, and one fewer, once actually checked against reality. **Corrected before deleting**: `mobile/MobileChartModal.tsx` was on the original removal list (both here and in `DESIGN_BRIEF.md`'s superseded-files table) but is still genuinely live — `App.tsx` imports and renders it, wired to the mobile "tap a watchlist row to view its chart" feature, with 5 real importers; no task in this redesign ever built a unified chart view to replace it (charting was out of scope throughout), so deleting it would have broken a real, working feature. Left it in place and corrected both docs. **Deleted, each verified with a zero-importer grep first**: the 5 originally-listed `mobile/MobileHomeView.tsx`, `MobileWatchlistView.tsx`, `MobilePortfolioView.tsx`, `MobileProfileView.tsx`, `MobileOrderModal.tsx`, plus `mobile/MobileBottomNav.tsx`, `GrowwHeader.tsx`, `GrowwSubNav.tsx` (all three superseded by `AppShell` back in Task 5, per that task's own completion note — never actually removed until now), `GrowwHoldingsView.tsx`, `PortfolioAnalyticsView.tsx` (both superseded by Task 11), `ClientProfileView.tsx`, `UserProfileModal.tsx`, `LinkPeAddFundsModal.tsx`, `CustomerSupportModal.tsx` (all four superseded by Task 12), and `TradingTerminal.tsx` (the dead file Task 13 discovered had zero importers from day one, not superseded by anything — just never wired up). The three other named cleanup items (`FO`-as-no-op branch, `MUTUAL_FUNDS` type member, unreachable mobile `activeMobileTab` states) were already gone — confirmed by grep returning zero matches outside the files just deleted — as an incidental side effect of Task 1's router rewrite and the `GrowwHeader.tsx` deletion (the last place `MUTUAL_FUNDS` was still referenced). _Depends on: every Core UI task above being confirmed working — all done. Deletes the 15 files above. Modifies: `AppShell.tsx`/`App.tsx` (stale comments pointing at now-deleted files updated), `DESIGN_BRIEF.md` (`MobileChartModal` correction)._ CSS bundle dropped 108.56KB → 91.91KB (Tailwind's JIT scans source files directly, so deleted files' utility classes stop being generated — the clearest confirmation this was real dead weight, not just fewer files). Verified via a full Playwright sweep of all 15 client routes on both desktop (1440px) and mobile (390px) viewports post-deletion — every route renders correctly, zero console/page errors — plus a clean `tsc` build confirming no stale imports anywhere._

## Responsive & Polish

- [x] **Cross-page responsive audit**: Swept all 8 rebuilt route families (Explore, Watchlist, Option Chain, Portfolio's 5 tabs, Profile's 8 tabs) at 375/768/1024/1440px via Playwright — zero horizontal overflow anywhere across the full matrix. Found one real "squeezed-desktop" tablet-tier failure exactly matching the pattern this task warned about: `OptionChainView.tsx` switched from its compact single-table CALLS/STRIKE/PUTS layout to the full multi-column desktop table at the `md` breakpoint (768px) — at 768px itself, the desktop table's ~11 columns (Volume/OI Change/OI/LTP ×2 sides + Strike) don't fit, silently clipping the entire PUTS side with no visible scroll affordance. The compact table (already a real, well-designed medium-density layout — Volume | Call LTP | Strike | Put LTP | Volume, not a dumbed-down mobile view) was moved to switch at `lg` (1024px) instead of `md` (768px), so the 768-1023px tablet tier gets the layout actually built to fit that width; confirmed via screenshot that both 768px (compact, both sides fully visible) and 1024px (full desktop table, still fits with no clipping) now render cleanly. **Touch targets**: found 9 interactive elements under 44×44px at 375px on a single sampled page (Portfolio → Positions) — traced to two shared primitives, not one-off misses: `Button`'s `sm` (32px) and `md` (40px) sizes, and `Tabs`' pill buttons (~30px), used everywhere in the redesign. Since the brief's 44px rule is itself explicitly scoped to "below `md`" (`DESIGN_TOKENS.md`), fixed both with a responsive `min-h-[44px] md:h-*` pattern — mobile gets the full touch-target floor, desktop keeps its original compact height exactly as before (confirmed via screenshot: zero visual change on desktop). Re-swept after the fix: 0 elements under 44×44px. _Modifies: `OptionChainView.tsx` (breakpoint), `ui/Button.tsx`, `ui/Tabs.tsx` (responsive min-height)._
- [x] **Accessibility pass**: **Contrast**: computed real WCAG contrast ratios for `--call-accent`/`--put-accent` against both surfaces they're actually used on as *text color* (`OptionChainRow.tsx`'s option-premium cells, not decoration) — light theme's `--call-accent` (#0891b2) measured 3.36-3.68:1, and dark theme's `--put-accent` (#a855f7) measured 3.70:1 against `--bg-surface-elevated`, both below the 4.5:1 AA minimum for normal text. Darkened/lightened each by the minimum needed to clear 4.5:1 while keeping the same hue (`--call-accent` light → `#077792`, 4.72:1; `--put-accent` dark → `#b46df8`, 4.52:1) — every other theme/token combination already passed. **Focus visibility**: found `--shadow-focus` was only ever applied via the shared `Button` component's own Tailwind classes — every plain `<button>` across the app (all the icon-only close/clear/stepper controls found below) had *no visible focus ring at all* when tabbed to, a real app-wide WCAG 2.4.7 gap `DESIGN_TOKENS.md` flagged as a requirement but nothing had actually enforced universally. Fixed with one global `:focus-visible` rule in `index.css` covering every focusable element regardless of which component rendered it, rather than patching each occurrence — verified via a real `Tab` keypress that a previously-invisible plain button now shows a real `box-shadow`, not `none`. **Screen-reader labels**: found 10 icon-only controls with no accessible name — dismiss/clear-search ×'s in `OrdersPositionsView`/`GrowwWatchlistView`/`OptionChainView` (×4), the lot-quantity +/- steppers in `OrderPreviewModal`, and three `AppShell` controls (mobile search icon, theme toggle — had a `title` but no `aria-label`, which isn't equivalent — and the Terminal-toggle button, icon-only below the `2xl` breakpoint where its text label is hidden). All given `aria-label`s. **Keyboard navigation**: re-verified (not just trusted from Task 4) that `Dialog`'s Escape-to-close and `Tabs`' focus-visible ring both still work correctly with the new global CSS rule layered in; `DataTable` row actions are plain `Button`/`<button>` elements with no custom key handling, so they inherit standard tab-order/activation for free. _Modifies: `index.css` (2 token values + global focus-visible rule), `OrdersPositionsView.tsx`, `GrowwWatchlistView.tsx`, `OptionChainView.tsx`, `OrderPreviewModal.tsx`, `AppShell.tsx` (aria-labels)._ Verified via Playwright: zero overflow across the full 4-breakpoint × 8-route matrix (both before and after fixes), 0/9 touch-target violations post-fix, a real focused-element check confirming a non-`none` box-shadow after `Tab`, and Dialog Escape-to-close still passing. Zero console/page errors throughout._

## Review

- [x] **Design review**: Ran `/design-review` against `DESIGN_BRIEF.md`/`INFORMATION_ARCHITECTURE.md`/`DESIGN_TOKENS.md`, with 25 fresh Playwright screenshots (7 pages × 3 breakpoints + a 4-page dark-theme pass) rather than relying on each task's own prior verification — full findings and screenshot index in `DESIGN_REVIEW.md`. **Zero Must-Fix findings**: no broken functionality, no accessibility blocker, no theme-ignoring surface turned up anywhere sampled — the brief's core complaints (F&O dead pill, Profile/KYC drift, dark-mode-ignoring screens) are genuinely resolved, not just claimed. **One real Should-Fix left open for a decision, not silently changed**: tablet-*portrait* still gets the desktop top nav instead of the bottom tab bar the brief explicitly names for that case (`AppShell.tsx`'s breakpoints are width-only, no orientation check) — confirmed visually via the 768×1024 screenshot; flagged with a concrete Tailwind `portrait:`/`landscape:` fix path since changing the primary nav shell's behavior felt like the user's call, not this review's to make unilaterally. **Two Should-Fix findings fixed inline during the review** (small, safe, same pattern as every prior task): `OptionChainView.tsx` had two spots still using raw `emerald`/`rose` Tailwind classes for genuine gain/loss meaning instead of `--gain`/`--loss` tokens, missed by Task 9's original conversion pass — both corrected, rebuilt, and re-verified with a full 15-route × 2-viewport smoke sweep (zero console errors). Also caught and fixed two stale cross-references in `INFORMATION_ARCHITECTURE.md` itself (still said "Portfolio's 4 tabs" in two spots — it's been 5 since Task 10; still referenced the now-deleted `LinkPeAddFundsModal`). Three Could-Improve notes recorded (zero-P&L rendering green with a `+` prefix; a couple of purely decorative hardcoded colors; the Option Chain's Long/Short-Buildup indicator color) — none blocking, all documented for whoever picks up further polish. _Modifies: `OptionChainView.tsx` (2 token fixes), `INFORMATION_ARCHITECTURE.md` (2 stale references). New: `DESIGN_REVIEW.md`, `screenshots/` (25 images)._
